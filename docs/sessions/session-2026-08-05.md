## 00:15 작업 (Vercel 배포 — vercel.json 추가 + 빌드 에러 수정)
**요청:** "vercel.json 추가해줘. 그리고 vercel에 올릴 때 루트 디렉토리는 그냥 './' 이걸로 하면 될까?" → 이어서 Vercel 실제 배포 시도 중 빌드 실패 로그 공유("src/routes/OrderNew.tsx(293,7): error TS6133 'productTypeLabel' is declared but its value is never read." 등 2건) → "수정 추가된 코드 깃허브에 올려줘"
**작업 내용:**
- 저장소 루트(`app/`)가 곧 `package.json` 위치임을 `git rev-parse --show-toplevel`로 재확인 — Vercel Root Directory는 `./`로 안내
- `vercel.json` 신규 추가 — `BrowserRouter` 사용 중이라 SPA 하위 경로 새로고침 시 404 방지용 rewrite(`"/(.*)" → "/index.html"`)
- Vercel 빌드 실패(`tsc -b` 에러 2건) 원인 파악: `src/routes/OrderNew.tsx`에 사용되지 않는 `productTypeLabel` 상수, `navigate`(`useNavigate` 결과), `ProductType` import가 이전 리팩터링 후 정리되지 않고 남아있었음 — 로컬에서 그동안 검증에 썼던 `npx tsc --noEmit`(단일 파일 모드)는 unused-symbol 에러를 잡지 못했고, Vercel이 실행하는 `npm run build`(`tsc -b`, 프로젝트 참조 모드)에서만 걸러짐. 셋 다 삭제
**결과:** 완료. `npx tsc -b` 클린 통과 확인 후, `rm -rf dist && npm run build`로 Vercel과 동일한 커맨드를 로컬 재현해 정상 빌드(`dist/` 산출물 생성, PWA service worker 포함) 확인. **향후 검증 시 `tsc --noEmit` 대신 `tsc -b`(또는 `npm run build`)를 기준으로 삼을 것** — 이번 빌드 실패의 직접 원인.

## 11:01 작업 (상품관리 검색 기능)
**요청:** (이전 요청) 상품관리 페이지에 상품명/모델코드/카테고리로 검색할 수 있는 기능 추가
**작업 내용:** `src/routes/AdminProducts.tsx`에 `search` state와 `filteredProducts` useMemo(이름/모델코드/카테고리 대소문자 무시 검색) 추가, 검색 input과 결과 개수/빈 상태 문구 추가. 기존 `grouped` useMemo가 `filteredProducts`를 기반으로 하도록 변경.
**결과:** 완료. Playwright로 모바일(151→7건, "황토" 검색)·데스크톱(모델코드 "HD-5" → 9건) 및 빈 결과 문구 확인. 스크린샷 `docs/sessions/screenshots/20260805-110124-*`.

## 11:06 작업 (갤러리 검색 기능)
**요청:** "갤러리 페이지에서도 동일하게 검색할 수 있게 해줘."
**작업 내용:** `src/routes/Gallery.tsx`에 AdminProducts.tsx와 동일한 패턴의 `search` state, `filteredProducts` useMemo, 검색 input, 결과 개수/빈 상태 문구 추가.
**결과:** 완료. Playwright로 데스크톱("위패" 151→3건, 빈 결과)·모바일(390px, "황토" 7건, 2열 그리드) 확인. 스크린샷 `docs/sessions/screenshots/20260805-110618-*`.

## 11:10 작업 (갤러리 비활성 상품 노출 버그 수정)
**요청:** "상품관리에서 비활성화된 상품은 갤러리 페이지에 보이지 않고 검색 결과로도 보이지 않아야 되는거 아냐??"
**작업 내용:** `src/routes/Gallery.tsx`의 상품 조회 쿼리에 `.eq('is_active', true)` 필터 추가(기존에는 `image_url` not-null 조건만 있어 비활성 상품도 노출되던 버그). `Products.tsx`는 이미 해당 필터가 있었음을 대조 확인.
**결과:** 완료. 실제 버그였음을 확인. 두 개의 브라우저 탭으로 관리자 탭에서 상품 비활성화 → 갤러리 탭에서 실시간으로 사라짐(새로고침 없이) 확인 후 재활성화하여 원상 복구. UI 변경이 아닌 로직 수정이라 스크린샷 생략.

## 11:24 작업 (파트너/관리자 목록 수정 기능 + 팀장관리 버튼 텍스트 축약)
**요청:** "등록된 파트너, 등록된 전체관리 목록도 수정할 수 있게 해줘. 그리고 팀장관리 목록에 '전체 현황 열람 권한 부여' 버튼 텍스트가 너무 길거든? 짧게 줄여줄래?"
**작업 내용:**
- `src/routes/AdminPartners.tsx`: `Modal` 컴포넌트를 재사용해 파트너사(장례회사명/사업자등록번호/대표 연락처) 인라인 수정 모달 추가. `organizations` 테이블에 대한 기존 `super_admin` UPDATE RLS 정책을 확인 후 클라이언트에서 직접 `.update()` 호출.
- `src/routes/AdminAccounts.tsx`: 동일한 방식으로 관리자(성함/연락처/역할/소속 회사) 수정 모달 추가. `AdminRow`에 `organization_id` 필드를 추가해 소속 회사 드롭다운이 현재 값으로 사전 선택되도록 함. 역할이 조직관리자일 때만 소속 회사 select가 나타나도록 조건부 렌더링. 본인 계정은 기존 관례대로 수정 버튼 비노출.
- `src/routes/AdminSalesReps.tsx`: "전체 현황 열람 권한 부여"/"전체 현황 열람 가능" → "권한 부여"/"열람 가능"으로 축약(모바일·데스크톱 두 곳).
**결과:** 완료. 세 파일 모두 `npx tsc -b` 통과. Playwright로 데스크톱·모바일 검증: 파트너사 수정(연락처 변경→저장→반영→원복), 관리자 수정(임시 테스트 조직관리자 계정 생성 → 수정 모달로 조직관리자→슈퍼관리자 변경 시 소속 회사 필드가 사라지고 저장 시 정상 반영되는 것 확인 → 테스트 계정 삭제로 원복), 팀장관리 버튼 텍스트 변경 및 토글 동작 확인(원복). 스크린샷 `docs/sessions/screenshots/20260805-112413-*`.

## 11:30 작업 (주문/갤러리/상품/주문작성 페이지 상단 네비게이션 추가)
**요청:** "oreder 페이지에서 네비게이션 상단 영역이 보이고 여기에 대시보드로 이동 버튼이 보이게 해줄래? 갤러리 페이지도. oreder/new 페이지, products 페이지"
**작업 내용:**
- `src/components/PageHeader.tsx` 신규 생성: `RoleLanding.tsx`의 기존 sticky 헤더 패턴을 재사용한 공용 컴포넌트. 로고("하미담")+`ThemeToggle(inline)`+"대시보드로" 버튼으로 구성, `sticky top-0 z-40`으로 스크롤 시에도 상단 고정.
- `src/routes/Orders.tsx`, `src/routes/Gallery.tsx`, `src/routes/Products.tsx`, `src/routes/OrderNew.tsx`: 기존에 페이지 본문 안에 파묻혀 있던 작은 "← 대시보드로" 텍스트 링크를 제거하고, 각 파일 최상단에 `<PageHeader />`를 배치. 파일 하나씩 수정 후 `npx tsc -b`로 순차 검증.
- `OrderNew.tsx`는 주문 접수 완료 후 별도로 렌더링되는 "주문서가 접수되었습니다" 확인 카드 화면이 있어, 그 화면의 `MoonMark`/`ThemeToggle`은 페이지 네비게이션이 아니므로 그대로 유지하고 건드리지 않음.
**결과:** 완료. 4개 파일 모두 `npx tsc -b` 통과. Playwright로 데스크톱(1440x900)·모바일(390x844) 양쪽에서 4개 페이지 모두 헤더가 겹침·잘림 없이 정상 렌더링되는 것 확인. 갤러리 페이지는 800px 스크롤 후에도 헤더가 상단에 고정되는 sticky 동작 확인. 스크린샷 `docs/sessions/screenshots/20260805-113000-*`.

## 13:50 작업 (슈퍼관리자 주문/판매 통계 페이지)
**요청:** "슈퍼관리자 페이지에서 주문 현황이랑 판매 현황 통계 구현하기로 했었지?" → 이전 세션에 `sales_rep.can_view_all_stats` 권한 필드와 `orders_select_stats_viewer` RLS 정책만 만들어두고 실제 통계 화면(RoleLanding.tsx의 "통계 화면은 준비 중입니다" 문구)은 미구현 상태였던 것을 확인 → 계획 수립 후 "필요한 기능들이 있다 판단되면 추가해줘"라는 위임을 받아 팀장별 판매 실적, 상품별 판매 순위, 기간 필터를 추가 범위로 포함해 진행.
**작업 내용:**
- DB 조사 결과 `orders` 테이블에 `orders_select_stats_viewer`(`current_can_view_all_stats()`) RLS 정책이 이미 존재해 `super_admin`과 `can_view_all_stats=true`인 `sales_rep` 모두 전체 주문 SELECT가 가능한 상태였음 — 마이그레이션 없이 프론트엔드만으로 구현 가능하다고 판단.
- `src/routes/OrderStats.tsx` 신규: 통계 핵심 컴포넌트. 기간 필터(오늘/이번주/이번달/전체) 토글, 상태별 주문 건수 카드 4개, 매출 요약(취소 제외), 조직별 판매 현황 표, 팀장별 판매 실적 표, 상품별 판매 순위 Top 5, 최근 주문 10건 리스트로 구성. `orders`를 organization/sales_rep/urn_product/tablet_product와 조인해 한 번에 조회 후 클라이언트에서 집계.
- `src/routes/AdminStats.tsx`(신규, `AdminShell`로 감싼 슈퍼관리자용 `/admin/stats` 래퍼), `src/routes/Stats.tsx`(신규, `PageHeader`로 감싼 `/stats` 래퍼 — `super_admin` 또는 `can_view_all_stats=true`인 `sales_rep`만 접근 허용하는 자체 가드 포함, 그 외에는 `/`로 리다이렉트)
- `src/components/DashboardIcons.tsx`에 `IconChart` 추가, `src/components/AdminShell.tsx` 사이드바 nav에 "주문 통계" 항목 추가, `src/routes/AdminHome.tsx`에 5번째 메뉴 카드 추가, `src/App.tsx`에 `/admin/stats`(SuperAdminRoute 그룹), `/stats`(ProtectedRoute 그룹) 라우트 추가
- `src/routes/RoleLanding.tsx`: `can_view_all_stats` 권한을 가진 팀장에게 뜨던 "통계 화면은 준비 중입니다" 안내 문구를 `/stats`로 이동하는 실제 링크로 교체
**결과:** 완료. `npx tsc -b` 통과. 테스트 슈퍼관리자 계정 로그인용 비밀번호가 세션/메모리 어디에도 남아있지 않아, 사용자 승인 하에 기존 `encrypted_password` 해시를 SQL로 임시 저장 → 임시 비밀번호로 교체해 로그인 검증 → 검증 완료 후 저장해둔 원래 해시값으로 정확히 복원(SQL로 해시 일치 재확인)하는 방식으로 진행. Playwright로 `/admin/stats`(1440px, 375px), `/admin`(신규 메뉴 카드), `/stats`(super_admin 가드 통과 확인) 검증 — 실제 주문 3건 기준 매출 2,160,000원, 팀장별/상품별 집계가 정상 렌더링되는 것 확인. `/stats`를 미권한 `sales_rep` 계정으로 접근 시 리다이렉트되는 케이스는 해당 권한을 가진 테스트 계정이 없어 별도 검증하지 못함(코드 로직상으로는 가드 조건에 포함됨). 스크린샷 `docs/sessions/screenshots/20260805-1355*`.

## 14:04 작업 (통계 페이지 폰트 변경 + 주문상세 상단 네비게이션 추가)
**요청:** "stats 페이지의 폰트를 그냥 프리텐다드로 바꿔주면 안될까? 그리고 주문 상세페이지에도 상단 네비게이션 영역이 보이게 해줘. 주문 목록으로 버튼도 상단 네비게이션 영역에 보이게해줘."
**작업 내용:**
- `src/routes/OrderStats.tsx`, `src/routes/Stats.tsx`: 제목·수치 등에 붙어있던 `font-serif-kr`(Noto Serif KR) 클래스를 모두 제거 — body 기본 폰트가 이미 Pretendard(`--font-sans-kr`)이므로 클래스만 빼면 자연히 적용됨.
- `src/components/AdminShell.tsx`: 다른 관리자 페이지(계정/상품/파트너/팀장 관리)의 제목은 그대로 세리프 유지가 필요해, `h1`에 `titleFont?: 'serif' | 'sans'`(기본값 `'serif'`) prop을 추가하는 방식으로 범위를 한정. `src/routes/AdminStats.tsx`에서만 `titleFont="sans"`로 지정.
- `src/components/PageHeader.tsx`: 뒤로가기 링크가 필요한 페이지를 위해 `backTo?: { to: string; label: string }` prop 추가(기존 5개 페이지는 prop 없이 그대로 호환).
- `src/routes/OrderDetail.tsx`: 기존에 본문 안에 있던 `MoonMark`+`ThemeToggle`+"← 주문 목록으로" 텍스트 링크 블록을 제거하고 `<PageHeader backTo={{ to: '/orders', label: '주문 목록으로' }} />`로 교체. 더 이상 쓰이지 않는 `MoonMark`/`ThemeToggle`/`Link` import 정리(`Link`는 `useParams`만 남기고 제거).
**결과:** 완료. `npx tsc -b` 통과(미사용 import 없음 확인). Playwright로 `/admin/stats`(폰트가 Pretendard로 정상 변경, 세리프체 잔존 없음)와 실제 주문 상세페이지(`/orders/12d520cc-...`)에서 상단 네비게이션에 "주문 목록으로" 링크가 배경음악·다크모드·"대시보드로"와 함께 정상 노출되는 것 확인. 스크린샷 `docs/sessions/screenshots/20260805-1404*`.

## 14:33 작업 (통계 페이지 — 파트너사 주문 / 팀장 주문 구분 필터)
**요청:** "주문통계에서 파트너사가 주문한 내용을 볼 수 있는 파트너사 전용 페이지는 어떻게 구현되고 있는거지? 또 슈퍼 관리자 페이지에서 파트너사가 주문한 내용과 일반 팀장들이 주문한 내용을 구분해서 볼 수 있는건?" → 조사 결과 공유(파트너사는 `/orders`를 공용으로 쓰되 `orders_select_own_org` RLS 정책이 자기 조직 주문만 보이게 자동 제한하는 구조이며, 슈퍼관리자 통계에는 조직별 표는 있었지만 "파트너사 주문 vs 팀장 주문"을 나누는 필터는 없었음) → AskUserQuestion으로 방식 확인 후 "주문자 역할 필터 추가"로 진행.
**작업 내용:** `src/routes/OrderStats.tsx`에 `Source`(`all`/`partner`/`individual`) 필터 추가. 주문 조회 쿼리에 `sales_rep:sales_rep_id(name, role)`로 `role` 필드를 함께 조회하고, 기존 `filtered` useMemo에 `sales_rep.role === 'org_admin'`(파트너사 주문) / `'sales_rep'`(팀장 주문) 조건을 기간 필터와 함께 결합 — 상태별 건수·매출 요약·조직별/팀장별 표·상품 Top5·최근 주문이 모두 자동으로 필터링된 결과를 반영하도록 함(별도 집계 로직 중복 없음). 기간 토글 아래에 "전체/파트너사 주문/팀장 주문" 토글 추가, 매출 요약 제목과 팀장별 실적 표 제목("팀장별 판매 실적" ↔ "파트너사 담당자별 판매 실적")이 필터에 맞춰 동적으로 바뀌도록 처리. DB 마이그레이션 불필요(role은 기존 profiles 컬럼).
**결과:** 완료. `npx tsc -b` 통과. Playwright로 `/admin/stats`에서 필터 토글 렌더링, "파트너사 주문" 선택 시 (현재 테스트 데이터엔 org_admin이 없어) 0건·빈 상태 문구가 정상 노출되는 것 확인 후 "전체"로 복귀. 실제 org_admin 계정으로 주문을 넣어보는 실데이터 검증은 테스트 파트너사 계정이 없어 미실시. 추가로 "파트너사/팀장 화면이 시각적으로 구분되면 좋겠다"는 의견에는, 별도 페이지 신설(과함) 대신 `Orders.tsx` 제목에 소속 조직명을 붙이는 가벼운 방식을 제안 — 사용자 확정 시 후속 작업으로 진행 예정. 스크린샷 `docs/sessions/screenshots/20260805-1433*`.

## 15:00 작업 (실제 파트너사 테스트 계정으로 주문 접수 → 통계 필터 실데이터 검증)
**요청:** "실제 파트너사 테스트 계정으로 주문을 넣어 재확인 테스트 해줘."
**작업 내용:**
- 테스트 슈퍼관리자(`test-super@hamidam-test.local`) 로그인용 비밀번호를 매번 임시 리셋→복원하는 대신, 사용자 승인(AskUserQuestion "영구 고정" 선택) 하에 `TestSuper2026!Fix`로 영구 고정(SQL `crypt()` 갱신, `encrypted_password = crypt(pw, encrypted_password)`로 재검증).
- 실제 UI 플로우로 파트너사 테스트 계정 생성: `/signup/partner`로 조직 "테스트파트너사" + 담당자 "테스트담당자"(`test-partner@hamidam-test.local` / `TestPartner2026!`) 가입 → 슈퍼관리자로 `/admin/partners`에서 "승인" 버튼 클릭해 `status: pending → approved` 전환.
- 파트너 계정으로 로그인해 `/orders/new`에서 실제 주문 접수 시도 → "주문서보내기"가 아무 반응 없이 멈추는 현상 발생. 원인 조사 결과 `src/components/Field.tsx`의 `<input required>`가 장례식장/장지 필드에도 적용되어 있었고, 두 필드를 비워둔 채 제출해 브라우저 네이티브 검증("이 입력란을 작성하세요")에 걸려 조용히 막힌 것이었음(앱 코드 버그 아님, 콘솔 에러도 없었던 이유). 장례식장/장지 값을 채워 재제출해 정상 접수 확인("주문서가 접수되었습니다" 화면 도달).
- 슈퍼관리자로 `/admin/stats` 재방문해 "파트너사 주문" 필터 실데이터 검증: 조직 "테스트파트너사" 880,000원 1건, 담당자 "테스트담당자" 880,000원 1건만 단독으로 필터링되고, "전체"에서는 팀장 주문(3건)과 합산되어 표시되는 것을 확인 — 필터 로직이 실제 org_admin 데이터로도 정확히 동작함을 검증.
**결과:** 완료. 신규 생성한 테스트파트너사/테스트담당자 계정과 주문 1건은 삭제하지 않고 재사용 가능한 테스트 픽스처로 남겨둠(기존 `test-super@...` 영구 고정 방침과 동일 기조). 스크린샷 `docs/sessions/screenshots/20260805-053700-after-stats-partner-filter-verified.png`.

## 15:38 작업 (Orders.tsx 제목에 소속 조직명 표시)
**요청:** "Orders.tsx 제목에 소속 조직명 표시" — 이전 대화에서 제안했던 후속 작업, 사용자 확정("바로 이어서 진행해줘")에 따라 진행.
**작업 내용:** `src/routes/Orders.tsx`에 `useAuth()`로 `profile`을 가져와, `profile.role === 'org_admin'`이고 `organization_id`가 있으면 `organizations` 테이블에서 이름을 조회해 `orgName` state에 저장. 제목을 `orgName ? \`${orgName} 주문 목록\` : '주문 목록'`으로 조건부 렌더링 — org_admin이 아닌 계정(슈퍼관리자/팀장)은 기존과 동일하게 "주문 목록"만 표시.
**결과:** 완료. `npx tsc -b` 통과. Playwright로 파트너 테스트 계정(`test-partner@hamidam-test.local`) 로그인 시 "테스트파트너사 주문 목록"으로 정상 표시되는 것 확인, 슈퍼관리자 계정으로도 재확인해 기존과 동일하게 "주문 목록"만 뜨는 것(회귀 없음) 확인. 스크린샷 `docs/sessions/screenshots/20260805-053830-after-orders-org-name-title.png`.

## 15:43 작업 (통계 페이지 — 전체 주문 상세 테이블로 교체)
**요청:** "주문 통계 내용에서 전체 주문내역을 표형식으로 전체 파악할 수 있게 만들어 줄수도 있어? 관리자에게 전문적인 통계 파악 내용처럼 보여줄 수 있게..." → AskUserQuestion으로 범위 확인("기존 '최근 주문' 10건 리스트를 교체" vs "별도 섹션 추가") 후 "기존 섹션 교체(권장)" 선택.
**작업 내용:** `src/routes/OrderStats.tsx`의 "최근 주문"(10건, 고인명+상태만 표시) 섹션을 "전체 주문 내역" 섹션으로 전면 교체.
- 컬럼: 접수일 / 고인명 / 소속 / 담당자 / 역할(파트너·팀장) / 금액 / 상태 / 상품 — `filtered`(기간·주체 필터가 이미 적용된 결과) 전체를 건수 제한 없이 표시, 상단에 "총 N건" 카운트 표기.
- 컬럼 헤더(상품 제외) 클릭 시 해당 기준으로 정렬 토글(`SortKey`/`SortDir` state + `sortedOrders` useMemo), 같은 헤더 재클릭 시 오름차순/내림차순 반전, 헤더에 ▲/▼ 표시.
- 행 클릭 시 `useNavigate`로 `/orders/:id` 상세 페이지 이동(기존 `Orders.tsx` 데스크톱 테이블과 동일한 패턴).
- 넓은 테이블이 모바일에서 페이지 레이아웃을 깨지 않도록 `overflow-x-auto` 컨테이너로 감쌈.
- 더 이상 쓰이지 않는 `recentOrders`/`Link` import 제거, `formatWon`/`orderAmount` 등 기존 헬퍼 재사용.
**결과:** 완료. `npx tsc -b` 통과. Playwright로 `/admin/stats`에서 전체 4건이 컬럼별로 정확히 표시되는 것 확인, "금액" 헤더 클릭 시 1,080,000→880,000→880,000→200,000원 순 내림차순 정렬 확인, 375px 모바일 뷰포트에서도 테이블이 카드 영역 내부에서만 가로 스크롤되고 페이지 레이아웃이 깨지지 않는 것 확인. 스크린샷 `docs/sessions/screenshots/20260805-0543*`.

## 15:50 작업 (통계 페이지 — 기간·주체 필터 한 줄 메뉴로 통합)
**요청:** (스크린샷 첨부) "주문통계 페이지의 이 버튼들을 메뉴처럼 만들어 줄래? 한줄로 쭉 보이게?" → AskUserQuestion으로 "두 그룹을 한 줄에 합치기" 확인 후 진행.
**작업 내용:** `src/routes/OrderStats.tsx`에서 기간 필터(오늘/이번주/이번달/전체)와 주체 필터(전체/파트너사 주문/팀장 주문)가 각각 `flex flex-wrap`인 별도 두 줄로 렌더링되던 것을, `overflow-x-auto`가 걸린 하나의 flex 컨테이너로 합치고 중간에 `h-6 w-px bg-border` 세로 구분선을 넣어 한 줄 메뉴처럼 보이도록 변경. 버튼에 `shrink-0 whitespace-nowrap`을 추가해 좁은 화면에서 버튼이 찌그러지지 않고 컨테이너 자체가 가로 스크롤되도록 처리.
**결과:** 완료. `npx tsc -b` 통과. Playwright로 1440px 데스크톱(7개 버튼이 구분선과 함께 한 줄에 모두 보임)과 390px 모바일(버튼이 잘리지 않고 컨테이너 하단에 가로 스크롤바가 나타나는 것) 양쪽 확인. 스크린샷 `docs/sessions/screenshots/20260805-0550*`.

## 15:58 작업 (통계 페이지 — KPI 카드 + 전기대비 증감률)
**요청:** 슈퍼관리자 주문 통계 페이지를 전문화된 대시보드 형태로 개선 — 신규 의존성 추가 없이, 기존 4개 상태 카드(접수/처리중/완료/취소)와 매출 요약을 하나의 KPI 카드 행으로 통합하고, 각 카드에 직전 같은 기간 대비 증감률(▲/▼) 표시 추가
**작업 내용:**
- `src/routes/OrderStats.tsx`
  - `previousPeriodBounds(period)`: 오늘→어제, 이번주→지난주, 이번달→지난달 경계 계산 (전체 기간은 비교 대상 없음, `null` 반환)
  - `previousPeriodLabel`: 기간별 비교 라벨 텍스트 ("어제", "지난주", "지난달")
  - `previousFiltered`/`previousStatusCounts`/`previousRevenue`: 현재 `source` 필터를 동일하게 적용한 직전 기간 집계
  - `trendPercent`/`trendCount`: 매출은 퍼센트, 건수는 증감 건수로 표시하는 증감률 계산 헬퍼
  - `TrendBadge` 컴포넌트: ▲(상승, 초록) / ▼(하락, 빨강) / －(변동없음, 회색) 아이콘 + 텍스트
  - 기존 "4개 상태 카드 그리드" + "매출 요약 카드"를 "핵심 지표" 단일 그리드로 통합 (매출 카드는 강조 테두리, 모바일에서 2칸 전체너비)
  - `period === 'all'`일 때는 비교 기준이 없으므로 "N 대비" 라벨과 증감 배지 모두 자동 숨김
- Tailwind CSS만 사용, 신규 npm 의존성 추가 없음
**결과:** 완료. `npx tsc -b` 클린 통과. Playwright로 데스크톱(1440x900)·모바일(390x844) 확인 — 이번달 기준 지난달 대비 매출 "▲ 신규", 접수 "▲ +4건" 정상 표시, 전체 기간 선택 시 비교 배지 자동 숨김 확인. 스크린샷: `20260805-060200-after-stats-kpi-cards-desktop.png`, `20260805-060300-after-stats-kpi-cards-mobile-fixed.png`

## 16:07 작업 (통계 페이지 — 전체 주문 내역 전체화면 모달)
**요청:** "전체 주문 내역" 표가 페이징 처리되어 있는지 확인, 그리고 이 표만 전체화면으로 볼 수 있게 해달라는 요청 (모달 오버레이 방식 선택)
**작업 내용:**
- 페이징 여부 확인: 현재 페이징 없이 `sortedOrders` 전체를 한 번에 렌더링 중임을 확인 (별도 수정 요청 없어 페이징 로직은 추가하지 않음)
- `src/routes/OrderStats.tsx`
  - `fullscreen` 상태 추가, ESC 키 입력 시 자동 닫힘 + 모달 열림 중 `document.body.style.overflow = 'hidden'`으로 배경 스크롤 잠금 (닫힐 때 원복)
  - 테이블 JSX를 `renderOrdersTable()` 함수로 추출해 인라인 카드와 모달에서 재사용 (중복 제거)
  - "전체 주문 내역" 카드 헤더에 "전체화면" 버튼 추가
  - `fixed inset-0` 전체화면 오버레이 모달: 헤더(제목 + 총 건수 + 닫기 버튼) + 표 영역(`overflow-auto`)으로 구성, 사이드바/다른 카드 없이 표만 크게 표시
**결과:** 완료. `npx tsc -b` 클린 통과. Playwright로 데스크톱(1440x900)·모바일(390x844) 확인 — 모달 정상 오픈, ESC로 정상 닫힘, 모바일에서도 가로 스크롤 가능한 표로 정상 표시. 스크린샷: `20260805-060800-after-stats-fullscreen-modal-desktop.png`, `20260805-060830-after-stats-fullscreen-modal-mobile.png`

## 15:21 작업 (가입 알림 트리거 + 푸시 검증 + PWA 설치 유도)
**요청:** "알림 트리거부터 추가하고 알림 켜기 푸시 검증을 하자. 그리고 각 사용하는 기기별로 하미담 앱을 실행하면 쉽게 홈 화면에 추가할 수 있도록 모달창이나 팝업창이 나타나 쉽게 설치할 수 있도록 도와줄 수 있는 방법도 구현해보자." (파트너사 가입만 알림 대상으로 범위 확정)
**작업 내용:**
- (1단계) Supabase에 `notify_new_signup()` 트리거 함수 + `profiles` 테이블 `AFTER INSERT` 트리거 추가 (role='org_admin' AND status='pending'일 때만 발동), 기존 `order-notify` 엣지함수에 `type: 'new_signup'` 분기 추가하여 활성 super_admin에게 푸시 전송 (파트너사명 + 담당자명, `/admin/partners`로 딥링크)
- (2단계) "알림 켜기" 푸시 구독 → 실제 알림 수신까지 end-to-end 검증
  - Playwright로 브라우저 컨텍스트에 notifications 권한 부여 후 test-super 계정 로그인 → "알림 켜기" 클릭 → `push_subscriptions`에 구독 row 정상 생성 확인
  - 실제 `/signup/partner` 폼으로 신규 파트너사 가입 시나리오 재현 → DB 트리거가 `order-notify` 엣지함수(v2)를 200으로 호출한 로그 확인 → super_admin 브라우저의 서비스워커가 실제로 push 이벤트를 수신해 `showNotification()`으로 "새 파트너사 가입 신청 / 푸시테스트장례 (푸시테스트담당자) 님이 가입 승인을 기다리고 있습니다" 알림을 등록한 것을 `registration.getNotifications()`로 확인 (딥링크 url: `/admin/partners`)
  - 검증에 사용한 테스트 파트너사(가입 계정·조직·profile)는 확인 후 정리 삭제함
- (3단계) 기기별(Android/iOS/데스크톱) PWA 홈 화면 추가 유도 모달 구현
  - `src/components/InstallPrompt.tsx` 신규 작성: `RoleLanding` 진입(로그인 후) 3초 뒤 하단 바텀시트로 표시(AskUserQuestion에서 "로그인 후 자동 표시" 선택)
  - Android/Chrome/Edge: `beforeinstallprompt` 이벤트를 캡처해 기본 브라우저 프롬프트를 막고, "홈 화면에 추가" 버튼 클릭 시 `deferredPrompt.prompt()` 호출
  - iOS Safari: `beforeinstallprompt`가 발생하지 않으므로 User-Agent로 iOS 감지 후 "공유 버튼 → 홈 화면에 추가" 수동 안내 UI로 분기
  - 이미 standalone(설치됨) 상태면 표시 안 함, 닫기(✕) 클릭 시 `localStorage`에 `hamidam-install-dismissed` 저장해 이후 재노출 안 함, `appinstalled` 이벤트 시에도 동일하게 dismiss 처리
  - `RoleLanding.tsx`에 `<InstallPrompt />` 마운트
**결과:** 3단계 모두 완료. `npx tsc -b` 클린 통과. Playwright로 `beforeinstallprompt` 이벤트를 강제 발생시켜 Android 배너 정상 표시·"홈 화면에 추가" 버튼 확인, User-Agent를 iOS로 오버라이드해 공유 버튼 안내 UI 정상 표시 확인, 닫기 클릭 후 새로고침해도 재노출되지 않음(localStorage 플래그) 확인. 스크린샷: `20260805-063300-after-installprompt-android-mobile.png`, `20260805-063400-after-installprompt-ios-mobile.png`

## 16:46 작업 (PWA 설치 안내 다시보기 + 하단 시트 잘림 수정)
**요청:** "pwa 홈 화면 추가창 언제든지 다시 볼 수 있게도 할 수 있을까?? 그리고 하단에 나오는 가이드 팝업창 밑에가 잘려보이는것 같은데 이것도 확인해줘."
**작업 내용:**
- (하단 잘림 수정)
  - `index.html`: `<meta name="viewport">`에 `viewport-fit=cover` 추가 (기존엔 없어서 `env(safe-area-inset-*)`가 항상 0으로 무시됨)
  - `src/components/InstallPrompt.tsx`: 바텀시트 wrapper 패딩을 `pb-4 sm:pb-6` → `pb-[calc(env(safe-area-inset-bottom)+1rem)] sm:pb-[calc(env(safe-area-inset-bottom)+1.5rem)]`로 변경 — 홈 인디케이터가 있는 iPhone(특히 PWA standalone 모드)에서 시트 하단이 가려지지 않도록 안전영역만큼 여백 추가. env가 0인 환경(대부분 데스크톱/일반 브라우저)에서는 기존과 동일한 값으로 계산되어 회귀 없음
- (재열람 기능 — 사용자가 "햄버거 메뉴로 몰아넣자"고 제안)
  - `src/components/InstallPrompt.tsx`: 마운트 시 `hamidam-install-dismissed` 플래그가 있어도 `beforeinstallprompt`/iOS 감지 리스너는 항상 붙이도록 수정(기존엔 dismiss 시 아예 리스너 등록을 안 해서 이후 강제로 다시 띄울 수단이 없었음). 3초 자동표시 타이머만 "dismiss 안 됐고 standalone 아닐 때"로 게이팅. `hamidam:show-install-prompt` 커스텀 이벤트 리스너 + `openInstallPrompt()` export 함수 추가 — 호출 시 dismiss 여부와 무관하게 즉시 표시
  - `src/components/HeaderMenu.tsx` 신규 작성: 햄버거 아이콘 버튼 + 드롭다운(배경음악 토글, 다크모드 토글, "홈 화면 설치 안내 다시 보기", 로그아웃). 바깥 클릭 시 자동 닫힘
  - `src/routes/RoleLanding.tsx`: 헤더 우측의 개별 BGM/테마/로그아웃 버튼 3개를 `<HeaderMenu onSignOut={signOut} />` 하나로 교체 (로고 + 햄버거만 남도록 정리)
**결과:** 완료. `npx tsc -b` 클린 통과. Playwright(모바일 390x844, test-super 계정)로 확인 — 헤더가 로고+햄버거로 정리됨, 메뉴 클릭 시 드롭다운(배경음악/다크모드/설치안내 다시보기/로그아웃) 정상 표시, `hamidam-install-dismissed=1`로 강제 설정한 상태에서도 자동표시는 안 뜨지만 메뉴의 "홈 화면 설치 안내 다시 보기" 클릭 시 즉시 재노출됨을 확인. 시트의 `padding-bottom` computed 값이 env(safe-area-inset-bottom)=0인 테스트 환경에서 기존과 동일(20px, root font-size 20px 기준 1rem)임을 확인해 회귀 없음도 검증(실제 안전영역 반영은 노치/홈 인디케이터가 있는 실기기에서만 차이 발생, 이 환경에서는 재현 불가). 스크린샷: `20260805-074653-after-headermenu-closed.png`, `20260805-074706-after-headermenu-open.png`, `20260805-074759-after-installprompt-reopened-ios.png`

## 16:52 작업 (햄버거 메뉴를 전체 페이지 헤더로 확대 적용)
**요청:** "햄버거 메뉴는 모바일 화면크기의 모든 상단 네비게이션 섹션에 메뉴들이 보이는 페이지에 동일하게 적용되게 해야해."
**작업 내용:**
- `src/components/HeaderMenu.tsx`: RoleLanding 전용이던 컴포넌트를 범용화 — `links`(페이지별 이동 링크 배열), `onSignOut`(있을 때만 로그아웃 항목 표시), `showBgm`(기본 true) prop 추가. 드롭다운 컨테이너를 `divide-y divide-border`로 바꿔 항목 조합이 달라져도 구분선이 자동으로 맞도록 정리
- `src/components/PageHeader.tsx`: 갤러리/주문목록/상품목록/주문상세/통계/주문작성 등에서 공용으로 쓰는 헤더. 기존 BGM+테마+"대시보드로"(+선택적 backTo) 개별 버튼들을 `<HeaderMenu links={[...backTo?, {to:'/', label:'대시보드로'}]} />` 하나로 교체
- `src/components/AdminShell.tsx`: 슈퍼관리자 콘솔의 모바일 전용(`lg:hidden`) 상단바 — 기존 홈 아이콘+테마+로그아웃 개별 버튼을 `<HeaderMenu links={[{to:'/', label:'메인 대시보드로'}]} onSignOut={signOut} showBgm={false} />`로 교체 (원래 BGM 컨트롤이 없었으므로 유지). 데스크톱 사이드바 레이아웃(`lg:` 이상)은 이미 라벨이 다 보이는 넓은 UI라 변경하지 않음
- 미사용 정리: `AdminShell.tsx`에서 더 이상 안 쓰는 `IconLogout` import 제거 (`IconHome`은 데스크톱 사이드바에 계속 쓰여 유지)
**결과:** 완료. `npx tsc -b` 클린 통과. Playwright(모바일 390x844)로 확인 — `/gallery`(PageHeader)에서 메뉴 클릭 시 "대시보드로 / 배경음악 / 다크모드 / 설치 안내 다시 보기" 정상 표시(로그아웃 없음, 기존과 동일), `/admin`(AdminShell 모바일 헤더)에서 "메인 대시보드로 / 다크모드 / 설치 안내 다시 보기 / 로그아웃" 정상 표시(BGM 없음) 확인. 스크린샷: `20260805-075215-after-adminshell-headermenu-open.png`

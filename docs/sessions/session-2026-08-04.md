# 세션 로그 2026-08-04

## 09:20 작업
**요청:** 주문서 작성 화면의 각인 미리보기가 너무 작음 — 더 크게 보고 싶고, 위치·크기·폰트(무료 상업용 한글 폰트)도 조정하고 싶다.
**작업 내용:**
- `orders` 테이블에 유골함/위패 각인 오버라이드 컬럼 8개 추가(위치 x/y, 글자크기, 글꼴 — 모두 nullable, null이면 상품 기본값 사용)
- `index.html`에 무료 상업용 한글 폰트 4종 추가 로드(나눔명조/나눔고딕/송명체/고운바탕, 기존 Noto Serif KR 유지)
- `types.ts`에 `ENGRAVE_FONTS`, `DEFAULT_ENGRAVE_FONT` 추가, `Order` 타입에 오버라이드 필드 반영
- `EngravePreview` 컴포넌트에 `fontFamily` prop 추가
- `OrderNew.tsx`: 기존 소형 정적 미리보기를 `EngraveEditor`(확대된 미리보기 + 클릭으로 위치 이동 + 글자크기 슬라이더 + 글꼴 선택 + 기본값 되돌리기)로 교체, 제출 시 오버라이드 값 저장
- `OrderDetail.tsx`: 저장된 오버라이드 값(없으면 상품 기본값)으로 최종 각인 미리보기 표시 추가
**결과:** 완료. Playwright로 실제 동작 검증 — 유골함 미리보기 확대 렌더링, 이미지 클릭 시 위치 이동(74.8%, 24.6%), 글자 크기 슬라이더(12.0%), 글꼴 선택(나눔명조) 모두 정상 반영. 주문서 제출 후 DB에 8개 오버라이드 값 정확히 저장 확인, 주문 상세 페이지에서도 저장된 값 그대로 재현되는 것 확인. `npx tsc --noEmit` 통과.

## 09:40 작업
**요청:** 주문 접수 알림 구현. 슈퍼관리자가 주문 상태(처리중/완료/취소)를 변경하면 해당 주문을 등록한 팀장(판매자)의 기기에도 상태 변경 알림이 울리도록 구현.
**작업 내용:**
- Web Push(VAPID) 인프라 구축
  - `push_subscriptions` 테이블 신설(RLS: 본인 구독만 조회/등록/수정/삭제)
  - `pg_net` 확장 활성화, Supabase Vault에 VAPID 키·웹훅 시크릿 저장 후 `get_app_secret()` SECURITY DEFINER 함수로 service_role만 조회 가능하도록 구성
  - `orders` 테이블에 트리거 2개 추가: `AFTER INSERT`(신규 주문 접수) → 슈퍼관리자+해당 조직관리자에게 알림, `AFTER UPDATE OF status`(상태값 변경 시에만) → 주문 등록 팀장(`sales_rep_id`)에게 알림. 둘 다 `pg_net.http_post`로 Edge Function 비동기 호출
  - Edge Function `order-notify` 배포 — 웹훅 시크릿 헤더로 인증, 알림 유형별 수신자 결정, `web-push` 라이브러리로 발송, 만료된 구독(404/410) 자동 정리
- 프론트엔드
  - `vite-plugin-pwa`를 `generateSW` → `injectManifest` 전략으로 전환, 커스텀 서비스워커(`src/sw.ts`)에 `push`/`notificationclick` 이벤트 핸들러 작성
  - `src/lib/push.ts`(구독 생성/저장), `src/components/NotificationBanner.tsx`(대시보드 상단 알림 켜기 배너, 사용자가 명시적으로 클릭해야 권한 요청 — 자동 팝업 지양) 신규 작성, `RoleLanding.tsx`에 배너 삽입
  - `vite.config.ts`에 `devOptions: { enabled: true, type: 'module' }` 추가 — 기본값으로는 개발 서버에서 서비스워커가 등록되지 않아 로컬 테스트/디버깅이 불가능했음
**결과:** 완료. DB 트리거→`pg_net`→Edge Function 파이프라인은 실제 주문 상태 변경으로 트리거를 발화시켜 `net._http_response`에서 상태 코드 200 응답 확인, curl 직접 호출로도 인증·수신자 조회 로직 정상 동작 확인(구독자 없어 `sent:0`). 프론트엔드는 Playwright로 서비스워커 정상 등록(devOptions 추가 후) 및 대시보드 배너 노출 확인, "알림 켜기" 클릭 시 `Notification.requestPermission()`이 정상 호출되는 것까지 확인. 단, 브라우저 네이티브 권한 승인 대화상자는 페이지 DOM 밖의 브라우저 UI라 자동화 도구로 클릭할 수 없어(의도된 브라우저 보안 동작) 실제 구독 생성 및 실제 기기 알림 수신까지는 자동 검증하지 못함 — 사용자가 실제 브라우저/PWA에서 "알림 켜기"를 눌러 권한을 허용하는 수동 테스트가 필요함. `npx tsc --noEmit` 통과.

## 10:00 작업
**요청:** 태블릿PC/노트북처럼 화면이 넓은 기기에서는 모바일 폭 그대로 쓰지 말고 화면을 좌우로 분할해서 활용하고 싶다. 5단계 계획(로그인/가입 → 주문작성/상세 → 목록형 화면 → 슈퍼관리자 콘솔 네비게이션 → 갤러리/대시보드) 중 Phase 1(로그인/회원가입)부터 진행.
**작업 내용:**
- `AuthLayout.tsx`: `md`(≥768px) 이상에서 좌(브랜드 패널: `MoonMark` + "하미담" + 소개 문구) / 우(로그인·가입 폼 카드) 좌우 분할 레이아웃으로 전환. `Login.tsx`, `SignupSalesRep.tsx`가 공용으로 이 컴포넌트를 쓰므로 두 화면 모두 자동 적용됨. 모바일(`md` 미만)은 기존과 동일하게 상단에 로고+제목이 오는 1단 레이아웃 유지
- (버그 수정) `MoonMark.tsx`: SVG `id`(`moonMarkGradient`, `moonMarkMask`)가 하드코딩되어 있어, 이번에 같은 화면에 `MoonMark`가 2개(좌측 브랜드 패널용 + 모바일 헤더용) 동시에 렌더링되자 id 중복으로 마스크가 깨져 아이콘이 안 보이는 문제 발견 → `useId()`로 인스턴스별 고유 id를 생성하도록 수정
**결과:** 완료. Playwright로 모바일(375px)/태블릿(820px)/노트북(1440px) 3개 폭 + 다크모드까지 스크린샷 검증. 태블릿 폭에서 브랜드 문구 줄바꿈이 어색해서 강제 줄바꿈(`<br/>`) 제거하고 자연스럽게 흐르도록 조정. `npx tsc --noEmit` 통과.

## 10:15 작업
**요청:** Phase 2(주문 작성/상세 화면) 진행 — `lg`(1024px) 이상에서 좌측 입력 폼 / 우측 각인 미리보기를 좌우 분할로.
**작업 내용:**
- `OrderNew.tsx`: 컨테이너 `max-w-2xl` → `lg:max-w-4xl xl:max-w-5xl`로 확장. `<form>`을 `lg:grid lg:grid-cols-[1fr_340px] lg:gap-6` 그리드 컨테이너로 전환, 기존 카드(`space-y-8 rounded-2xl...`)를 좌측 열로 감쌈. 기존 인라인 각인 미리보기 블록은 `lg:hidden`으로 모바일/태블릿 전용으로 남기고, 동일한 `EngraveEditor`(유골함/위패, `urnEngrave`/`tabletEngrave` state 공유)를 재사용하는 새 우측 열 블록을 `hidden lg:sticky lg:top-10 lg:block`으로 추가 — state를 공유하므로 로직 중복 없이 마크업만 두 곳에 렌더링
- `OrderDetail.tsx`: 동일 패턴 적용. 컨테이너 확장, `{order && (...)}` 내부를 `lg:grid lg:grid-cols-[1fr_340px] lg:gap-6`로 감싸고, 기존 인라인 각인 미리보기(`EngravePreview` 뷰 전용)는 `lg:hidden`, 새 우측 열 sticky 블록에 동일한 두 `EngravePreview` 호출을 추가
- `md`(768px, 태블릿)는 의도적으로 1단 유지 — 태블릿 세로 폭에서는 2단이 너무 좁아짐
**결과:** 완료. `npx tsc --noEmit` 통과(JSX 태그 균형 확인 겸함). Playwright로 실제 주문 작성 화면(상품 선택 + 고인명 입력해 각인 미리보기 활성화)과 기존 주문 상세 화면을 1440px/820px/375px에서 검증 — 1440px에서 좌우 분할 정상 동작(우측에 확대된 각인 미리보기+글자크기 슬라이더+글꼴 선택 표시), 820px/375px에서는 기존과 동일하게 1단 인라인 표시로 정상 폴백. 스크린샷 5장 저장.

## 10:26 작업
**요청:** Phase 3(목록형 화면) 진행 — `md`(768px) 이상에서 카드/리스트를 테이블로 전환.
**작업 내용:**
- 공통 패턴: 기존 카드/리스트 마크업은 `md:hidden`으로 모바일 전용 유지, 동일 state를 사용하는 `<table>` 마크업을 `hidden ... md:block`으로 신규 추가(렌더링만 이중화, 로직은 공유)
- `Orders.tsx`: 컨테이너 `max-w-3xl` → `md:max-w-4xl lg:max-w-5xl`. 고인명/주문자·상품/소속·담당자/상태/접수일 5열 테이블 추가, 행 클릭 시 `useNavigate`로 상세 페이지 이동(`<tr>`은 `<Link>`로 감쌀 수 없어 `onClick`+`cursor-pointer`로 대체)
- `AdminAccounts.tsx` / `AdminSalesReps.tsx` / `AdminProducts.tsx`: 모바일 리스트와 테이블이 공유하는 토글 버튼 클래스를 `toggleButtonClass(row)` 헬퍼로 추출해 조건부 스타일 중복 제거 후 각각 테이블 추가(이름/연락처/소속/역할/액션, 이름/연락처/소속/전체현황열람, 사진/상품명/모델/유형/가격/액션)
- `AdminPartners.tsx`: 장례회사명/사업자등록번호/연락처 3열 테이블 추가
- `Products.tsx`: 원래 계획은 테이블 전환이었으나, 이미 상품 이미지 카드 그리드라 테이블로 바꾸면 썸네일을 잃는 손해가 있어 설계 판단으로 제외 — 대신 컨테이너를 `xl:max-w-6xl`로, 그리드를 `xl:grid-cols-4`로 넓혀 와이드 화면 공간 활용도만 개선(사용자에게 사전 안내 후 진행)
**결과:** 완료. `npx tsc --noEmit` 통과. Playwright로 슈퍼관리자 계정(`test-super@hamidam-test.local`) 로그인 후 `/orders`, `/admin/accounts`, `/admin/partners`, `/admin/sales-reps`, `/admin/products`, `/products`를 1440px에서 전수 검증(테이블 렌더링·실데이터·뱃지·썸네일 정상, `Orders.tsx` 행 클릭 시 실제 주문 상세로 이동 확인) + 375px에서 `/admin/accounts`, `/admin/products`, `/orders` 모바일 폴백(테이블 숨김, 기존 카드/리스트 노출) 확인. 단, `AdminProducts.tsx` 모바일 카드 행(이미지+텍스트+수정+비활성화 버튼)이 375px에서 요소가 빽빽하게 눌리는 기존 UX 이슈를 발견 — Phase 3에서 새로 만든 마크업이 아니라 원래 있던 모바일 카드 구조 그대로이므로 이번 범위에서는 수정하지 않고 확인만 함. 스크린샷 9장 저장.

## 10:34 작업
**요청:** Phase 4(슈퍼관리자 콘솔 네비게이션) 진행 — `lg`(1024px) 이상에서 좌측 고정 사이드바 추가.
**작업 내용:**
- `AdminShell.tsx`는 6개 관리자 라우트(`AdminHome`, `AdminAccounts`, `AdminProducts`, `AdminProductForm`, `AdminPartners`, `AdminSalesReps`)가 공유하는 단일 레이아웃 컴포넌트라, 이 파일 하나만 수정해 전체 콘솔에 일괄 적용
- `lg:flex`로 켜지는 `<aside className="fixed inset-y-0 left-0 ... w-64">` 신설 — 상단 로고+"슈퍼관리자 콘솔"(`/admin`로 링크), 중단 내비게이션 4개(관리자 계정 관리/상품 관리/파트너사 관리/팀장 관리 — 기존 `AdminHome.tsx`의 메뉴 카드와 동일한 아이콘·경로 재사용), 하단 "← 대시보드로"
- `useLocation().pathname.startsWith(to)`로 현재 라우트에 해당하는 메뉴 항목에 강조 스타일(`bg-accent/15 text-accent`) 적용
- 바깥 컨테이너에 `lg:pl-72`를 추가해 본문이 사이드바 폭만큼 밀리도록 하고, 기존 모바일 헤더(로고+뒤로가기+제목)는 `lg:hidden` 처리, 대신 `lg` 이상에서는 제목만 `h1`으로 표시
- `ThemeToggle`은 기존처럼 우측 상단 고정이라 사이드바와 겹치지 않아 별도 조정 불필요
**결과:** 완료. `npx tsc --noEmit` 통과. Playwright로 1440px에서 `/admin`(사이드바+메뉴 카드 동시 노출), `/admin/products`(사이드바의 "상품 관리" 항목이 활성 강조되고 본문에 테이블이 사이드바 폭만큼 밀려 정상 표시) 확인. 820px/375px에서는 사이드바가 사라지고 기존 상단 헤더("← 대시보드로" + 제목)로 정상 폴백되는 것 확인. 스크린샷 4장 저장.

## 10:42 작업
**요청:** Phase 5(갤러리/대시보드 미세조정) 진행 — 5단계 반응형 계획의 마지막 단계.
**작업 내용:**
- `Gallery.tsx`: 컨테이너 `max-w-5xl` → `xl:max-w-6xl` 추가, 그리드 `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4`에 `xl:grid-cols-5` 추가(정사각 썸네일이라 컬럼을 늘려도 카드가 과도하게 커지지 않아 Products.tsx보다 한 칸 더 넓힘)
- `RoleLanding.tsx`: 메인 컨테이너 `max-w-xl` → `lg:max-w-2xl` 추가 — 대시보드가 넓은 화면에서 지나치게 좁게 떠 보이는 것을 완화하는 최소 조정(계획대로 "minor tweaks" 범위에서 컬럼 재배치 등 구조 변경은 하지 않음)
**결과:** 완료. `npx tsc --noEmit` 통과. Playwright로 1440px에서 `/gallery`(5열 그리드로 정상 확장) · `/`(대시보드 폭이 살짝 넓어져 카드 간격 자연스러움) 확인, 375px에서 두 화면 모두 기존과 동일한 레이아웃(갤러리 2열, 대시보드 1단)으로 회귀 없이 폴백되는 것 확인. 스크린샷 4장 저장.

## 10:50 작업
**요청:** "이전이 더 좋은것 같아... 지금 수정해준 phase 5는 별로야" — Phase 5 변경(갤러리 5열 그리드, 대시보드 `lg:max-w-2xl`)이 기존 디자인보다 못하다는 피드백.
**작업 내용:** `Gallery.tsx`(`xl:max-w-6xl`, `xl:grid-cols-5` 제거)와 `RoleLanding.tsx`(`lg:max-w-2xl` 제거)를 Phase 5 이전 상태로 되돌림. Phase 1~4(로그인/가입, 주문작성/상세, 목록형 화면 테이블, 슈퍼관리자 콘솔 사이드바)는 그대로 유지.
**결과:** 완료. `npx tsc --noEmit` 통과, 두 파일 모두 10:00 시점(Phase 5 진행 전) 코드와 동일.

## 11:05 작업
**요청:** (스크린샷 첨부) "다른게 문제가 아니라 이게 문제였어... 다크모드일때 상품 이름 텍스트가 잘 안보여" — 10:50 되돌리기로도 해결 안 된 실제 문제는 `/gallery`의 다크모드 상품명 텍스트 가독성 버그였음.
**작업 내용:**
- 원인 분석: `index.css`의 `--hamidam-foreground`가 라이트 모드에서는 `#2c2640`(진한 보라, 흰 배경에서 가독성 OK)이지만 다크모드(`.dark` 셀렉터 + `prefers-color-scheme: dark` 두 블록 모두)에서는 `#f6ecd9`(밝은 크림색)로 바뀜. `Gallery.tsx`의 상품 카드는 `bg-white`로 배경이 테마와 무관하게 항상 흰색으로 고정되어 있는데, 그 위의 상품명 텍스트만 테마 반응형 클래스 `text-foreground`를 쓰고 있어 다크모드에서 밝은 크림색 글자가 흰 배경 위에 거의 안 보이는 상태였음
- `Gallery.tsx` 77번째 줄: 상품명 `<p>`의 클래스를 `text-foreground` → `text-neutral-800`(테마 무관 고정 짙은 회색)으로 변경
**결과:** 완료. `npx tsc --noEmit` 통과. Playwright로 다크모드 `/gallery`에서 상품명("S기독교황토(3중)" 등)이 흰 카드 위에 뚜렷하게 보이는 것 확인, 라이트 모드에서도 기존과 동일하게 자연스러운 것 확인. 스크린샷 2장 저장.

## 12:00 작업
**요청:** ① 갤러리 화면에서 상품을 클릭하면 큰 이미지+상품정보를 볼 수 있게 해달라 ② 각인 미리보기에서 위패 선택 시 텍스트가 가로가 아니라 위패 형태에 맞게 세로로 배치되게 해달라.
**작업 내용:**
- ①: `/products/:id` 라우트와 `ProductDetail.tsx`(큰 이미지+상품정보+"이 상품으로 주문서 작성" 버튼)가 이미 구현되어 있고 `Products.tsx` 목록에서 이미 이 방식으로 연결되어 있던 것을 확인. `Gallery.tsx`의 상품 카드 `<div>`를 `<Link to={`/products/${p.id}`}>`로 교체(호버 시 살짝 떠오르는 트랜지션 추가)해 갤러리에서도 동일한 상세 화면으로 연결
- ②: `EngravePreview.tsx`에 `vertical?: boolean` prop 추가 — `true`일 때 텍스트 span에 `writingMode: 'vertical-rl'`, `textOrientation: 'upright'` 적용(한글이 세로로 눕지 않고 정자로 위→아래 배치됨). 위패(`type === 'tablet'`) 상품일 때 이 prop을 전달하도록 3곳 수정: `OrderNew.tsx`(주문서 작성 화면의 `EngraveEditor`, `product.type === 'tablet'`로 자동 판별), `OrderDetail.tsx`(주문 상세의 위패 각인 미리보기 2곳, `vertical` 고정 전달), `AdminProductForm.tsx`(관리자 상품 등록/수정 화면, `form.type === 'tablet'`로 자동 판별)
**결과:** 완료. `npx tsc --noEmit` 통과. Playwright로 `/gallery`에서 상품 클릭 → `/products/:id` 상세 화면 정상 이동 확인. `/orders/new`에서 위패(금선세라믹위패) 선택 후 고인명 "홍길동" 입력 → 위패 이미지 안에 세로로 "홍길동"이 자연스럽게 배치되는 것 확인. 스크린샷 3장 저장.

## 13:00 작업
**요청:** 모바일 화면에서 메인/서브 텍스트가 의미 없이 혹은 가독성 떨어지게 줄바꿈되는 문제 — 화면 크기에 맞게 한 줄로 예쁘게 보이도록 처리해달라.
**작업 내용:**
- 375px 모바일 화면(`/`, `/gallery`)에서 원인 확인: 브라우저 기본 줄바꿈 규칙이 한글을 글자 단위로 아무 데서나 끊어 "테스트 슈퍼관리자" / "님, 안녕하세요", "가족의 추" / "억입니다", "그 소" / "중한 가치를", "알려" / "드려요"처럼 단어 중간이 잘리는 것을 확인
- 화면마다 개별 클래스를 수정하는 대신 `index.css`의 `body` 선택자에 전역으로 `word-break: keep-all`(공백 없는 한글 어절 내부에서 줄바꿈 금지) + `overflow-wrap: break-word`(그래도 한 단어가 화면보다 길면 예외적으로만 줄바꿈, 컨테이너 밖으로 넘치는 것 방지) 2줄 추가 — 앱 전체 화면에 일괄 적용됨
**결과:** 완료. Playwright로 375px에서 재확인: `/`(대시보드) "테스트 슈퍼관리자님," / "안녕하세요"로 자연스럽게 개선, 알림 문구 "알려드려요"가 한 단어로 유지됨. `/gallery` "가족의" / "추억입니다.", "그" / "소중한 가치를 보존합니다."로 개선, 상품명 "S꽃난황토(3중)"도 한 줄로 정상 표시. `/products` 목록도 회귀 없음 확인. CSS 전역 변경 1줄(파일 1개)로 전체 화면에 적용되어 별도 컴포넌트 수정 불필요. 스크린샷 5장(전/후) 저장.

## 11:30 작업
**요청:** "파트너사 셀프 가입 화면부터 진행해줘" — ("이제 무슨 작업을 해야하지?"에 대한 답으로 제안했던 항목).
**작업 내용:**
- 착수 전 조사 중 중요한 사실 발견: `project_org_structure.md` 메모리에 남아있던 "파트너사는 자체 등록 신청+승인 방식"이라는 기록은 2026-08-03 **초반** 결정이었고, 같은 날 뒤늦게 사용자가 직접 "조직관리자 계정은 자율가입이 아니라 슈퍼관리자가 직접 생성"으로 뒤집었으며 그 결과 당시 만들었던 `SignupPartner.tsx`는 의도적으로 삭제됐던 이력을 `docs/sessions/session-2026-08-03.md`에서 확인. 낡은 메모리 때문에 잘못된 다음 작업을 제안했던 것 — 메모리 파일을 먼저 정정하고, 사용자에게 상황을 설명 후 진행 방향(관리자 직접생성 방식과 셀프가입 방식을 **병행**)을 재확인받아 진행함
- DB(Supabase, 마이그레이션 `partner_self_signup_with_approval`): `organizations`에 `type='partner_company' AND status='pending'` 조건의 셀프 INSERT RLS 정책 추가, `profiles`에 `role='org_admin' AND status='pending'` 조건의 셀프 INSERT RLS 정책 추가(기존 슈퍼관리자 전용/팀장 셀프가입 정책과 병행, 대체 아님). 승인/거부 처리용 SECURITY DEFINER 함수 `approve_partner_organization(uuid)`/`reject_partner_organization(uuid)` 신설 — 슈퍼관리자만 호출 가능하며 조직 상태와 해당 org_admin 프로필 상태를 한 트랜잭션으로 함께 변경(승인 시 organizations.status=approved + profiles.status=active, 거부 시 rejected/disabled)
  - `organizations.status`(pending/approved/rejected), `approved_at`, `approved_by` 컬럼과 `Organization` 타입은 08-03에 이미 만들어진 채 남아있어서 재사용함(삭제된 건 화면과 RLS 정책뿐이었음)
- 프론트: `SignupPartner.tsx` 신규(경로 `/signup/partner`) — `SignupSalesRep.tsx`와 동일한 클라이언트사이드 패턴(`auth.signUp` → `organizations` insert(status=pending, id 클라이언트 생성으로 RLS SELECT 문제 회피) → `profiles` insert(role=org_admin, status=pending)). `App.tsx`에 라우트 추가, `Login.tsx`에 "가입하기(파트너 장례회사)" 링크 추가. `AdminPartners.tsx`: 목록에 상태 뱃지(승인 대기/승인됨/거부됨) 추가, 승인 대기 건에 승인/거부 버튼 추가(위 RPC 호출). 기존 "신규 파트너사 등록"(관리자 직접생성, 즉시 approved) 폼은 그대로 유지
**결과:** 완료. `npx tsc --noEmit` 통과. Playwright로 전체 플로우 실제 검증: `/signup/partner`에서 가입 신청 → `/`에서 "승인 대기 중입니다" 화면 확인 → 영구 슈퍼관리자 테스트 계정으로 `/admin/partners` 진입, "승인 대기" 뱃지 확인 → 승인 버튼 클릭 → "승인됨"으로 즉시 반영 확인 → DB 조회로 organizations.status=approved, profiles.status=active 동시 반영 확인 → 새 계정으로 재로그인 시 조직관리자 대시보드 정상 진입 확인. 테스트로 만든 파트너사/계정은 검증 후 정리(영구 테스트 계정 2개는 그대로 보존). 스크린샷 1장 저장(`20260804-140000-after-adminpartners-pending.png`).

## 21:07 작업 (정정: 위패 사진 없을 때 재배치 범위를 종교기호+이름으로 축소)
**요청:** "위패에서 사진이 없을 때 종교 아이콘과 이름이 위로올라가는건 맞지만 출생 사망 년도는 그대로 동일하게 위치하면 좋겠어.. 똑같이 위로 올라오지 말고" — 위패에 사진이 없을 때 빈 공간만큼 4개 요소(이름/생년월일/사망년월일/종교기호)를 모두 위로 당기던 기존 로직이 과도했다는 정정.
**작업 내용:** `OrderNew.tsx`의 `defaultEngraveOverride`와 `OrderDetail.tsx`의 `tabletElements`에서 `noPhotoShift`(= `engrave_religion_y_pct - engrave_photo_y_pct`) 적용 대상을 `name`/`religion`으로만 축소, `birth`/`death`는 상품 기본 `yPct`를 그대로 사용하도록 수정(사진 없을 때도 생몰년월일 위치 고정).
**결과:** 완료. Playwright로 위패(사진 없음) 주문 작성 화면 및 저장 후 주문 상세 화면 모두에서 종교기호+이름만 위로 이동하고 생년월일/사망년월일은 원위치 유지되는 것 확인.

## 21:26 작업 (출생/사망년월일 좌우 시작 높이 대칭화 — 위패+유골함 공통)
**요청:** "출생 사망 일자의 좌우 시작 높이가 대칭되게 같으면 좋겠어... 이건 사진이 있을 때도 마찬가지고, 위패뿐만 아니라 유골함에 표시될때도 동일하게 적용되면 좋겠어."
**작업 내용:**
- 원인: 생년월일/사망년월일의 `yPct`는 상품 기본값이 이미 좌우 동일(위패 3종, 유골함 143종 전부 58)했지만, 렌더링이 텍스트 블록의 세로 "중앙"을 기준으로 배치되고 있어 사망일자 종교별 문구 글자수 차이(召天/善終=2자, 入寂/涅槃=2자, 기본 卒=1자)와 날짜 자릿수 차이 때문에 블록 높이가 달라져 시작 높이(윗변)가 어긋나 보였음
- `EngravePreview.tsx`의 `EngraveElement`에 `anchor?: 'center' | 'top'` 필드 추가 — `'top'`이면 `yPct`를 텍스트 블록의 맨 위 시작점으로 사용(글자 수와 무관하게 시작 높이가 항상 일치). 기본값은 기존과 동일한 `'center'`
- `OrderNew.tsx`(`EngraveEditor`), `OrderDetail.tsx`(위패/유골함 `elements` 배열 양쪽), `AdminProductForm.tsx`(`previewElements`) 4곳 모두에서 `birth`/`death` 요소에만 `anchor: 'top'` 지정 — 공용 컴포넌트라 이 4곳 수정으로 전체 화면(주문 작성/상세/관리자 상품 편집)에 일괄 적용됨
- anchor 전환에 따라 시각적 위치가 달라지므로 보정 SQL 필요 — 위패/유골함 각각 실제 화면에서 "크게 보기"로 확대 검증 후 결정: 위패는 `engrave_birth_y_pct`/`engrave_death_y_pct`를 58→**34**로 변경(3개 상품 전체), 유골함은 58→**42**로 변경(143개 상품 전체). 위패와 유골함의 값이 다른 이유: 위패는 하단 장식 테두리 안쪽 여백이 더 넓어 34에서도 잘 맞지만, 유골함은 34에서 상단 목 부분 장식(체인/보석)과 겹쳐 42로 별도 보정 필요 — "위패와 유골함은 캘리브레이션을 공유하지 않는다" 원칙 재확인
**결과:** 완료. Playwright로 검증: 위패(사진 있음/없음 둘 다), 유골함 2종(장식 많은 상품 S기독교황토(3중), 단순한 상품 본1호보석) 모두 "크게 보기" 확대 화면에서 出生/2026年 등 출생·사망 시작 글자가 좌우 동일 높이로 정렬되고 하단 테두리 밖으로 넘치지 않는 것 확인. 유골함 실주문 1건 생성→제출→주문 상세 페이지 재조회까지 전 구간에서 동일하게 정상 반영되는 것 확인 후 테스트 주문 삭제. `npx tsc --noEmit` 통과. 스크린샷 7장 저장(전/후 비교 포함).

## 21:41 작업 (사망 한자 좌우 순서 통일 + 위패 표기 스타일(한자/점) 토글 신설)
**요청:** (스크린샷 첨부) "왼쪽에 출생 한자 맞지? 그럼 오른쪽도 사망 한자로 시작해야 하는거 아냐?" → 사망일 한자 문구 순서가 "날짜+한자"(출생과 반대)였던 비대칭 확인. "좌우 순서가 같게 해줘. 그리고 분양함처럼 점으로 간단하게 미리보기 표시될 수 있도록 동일하게 적용해 줄래?" — 위패도 유골함처럼 한자/점 표기 스타일을 고를 수 있게 해달라는 요청.
**작업 내용:**
- `lib/engrave.ts`의 `formatDeathEngrave`(한자 표기용)를 `"${base} ${deathWording}"` → `"${deathWording} ${base}"`로 수정 — 출생(`出生 2009年...`)과 동일하게 "한자 라벨 먼저, 날짜 나중" 순서로 통일. 유골함/위패/관리자 상품 미리보기가 모두 이 함수를 공유하므로 한 곳 수정으로 전체 반영. 점 표기용 `formatDeathEngraveDot`은 이미 라벨이 먼저였어서 수정 불필요
- `orders` 테이블에 `tablet_date_style` 컬럼 신설(text, not null default 'hanja') — 기존 유골함 전용 `urn_date_style`과 동일 구조를 위패에도 병행 적용
- `OrderNew.tsx`: `tabletDateStyle` state, `tabletEngraveTexts`(한자/점 전환 계산) 추가, 위패 두 `EngraveEditor` 호출부(모바일 인라인+데스크톱 사이드바) 모두 `tabletEngraveTexts`로 교체, "위패 표기 스타일"(한자 병기/점 표기 간단) 토글 UI를 유골함 토글 바로 아래에 추가, 제출 payload에 `tablet_date_style` 반영
- `OrderDetail.tsx`: `Order` 타입/select 쿼리에 `tablet_date_style` 추가, `tabletElements`에 유골함과 동일한 `isDotStyle` 분기 로직 추가(저장된 스타일에 따라 한자/점 포맷터 전환)
**결과:** 완료. `npx tsc --noEmit` 통과. Playwright로 `/orders/new`에서 위패 선택 시 새 "위패 표기 스타일" 토글 노출 확인, 한자 모드에서 "出生 2009年 2月 4日"/"善終 2026年 8月 13日"(좌우 모두 라벨 먼저) 정상 표시, 점 모드 전환 시 "生 2009.02.04"/"善終 2026.08.13" 정상 표시 확인. 실제 주문 제출 폼은 이번 세션에 로그인 세션이 없어 제출 버튼이 동작하지 않아(기존부터 있던 별개 이슈로 추정, 이번 변경과 무관), 대신 SQL로 직접 테스트 주문 2건(위패 dot+유골함 hanja / 위패 hanja+유골함 dot 조합) 생성해 `/orders/:id` 상세 페이지에서 저장된 `tablet_date_style`/`urn_date_style` 값에 따라 각각 올바른 포맷으로 렌더링되는 것을 확인 후 테스트 주문 삭제. 스크린샷 1장 저장(`20260804-214539-after-tablet_date_style_toggle_and_hanja_order_fix.png`).

## 22:15 작업 (주문 상세 각인 미리보기 확대, 다크모드 달력 아이콘 잔여 버그, 위패/유골함 단독 주문 허용)
**요청:** ① "주문 상세 화면에서도 미리보기 각인 이미지를 크게 볼 수 있도록 해줘." ② (스크린샷 첨부) "여전히 다크모드일 때 이렇게 달력 아이콘이 어두워서 안보이는곳이 또 있네.. 잘 보이게 해줘." — 화장 날짜 및 시간(datetime-local) 필드. ③ "주문 할 때 위패 또는 분양함 하나만 선택해서 주문하게 할 수 도 있을까? 지금은 두개 다 주문해야 주문이 되는것 같아서..."
**작업 내용:**
- ①: `OrderNew.tsx`의 `EngraveEditor`에 이미 있던 "크게 보기" 패턴을 `OrderDetail.tsx`에도 이식. `Modal` 컴포넌트 import, `zoomedProduct: 'urn' | 'tablet' | null` state 추가, 모바일 그리드/데스크톱 사이드바 두 미리보기 캡션 옆에 "크게 보기" 버튼 추가, 공용 `Modal` 하나로 선택된 상품의 `EngravePreview`를 확대 렌더링(읽기 전용, 위치 편집 불가)
- ②: `index.css`의 다크모드 캘린더 아이콘 보정 CSS(`filter: invert(1)`)가 `input[type="date"]`에만 적용되어 있던 것을 `input[type="datetime-local"]`에도 동일 적용(`:root[data-theme="dark"]` 블록 + `prefers-color-scheme` 폴백 블록 둘 다 수정)
- ③: DB 확인 결과 `orders.urn_product_id`/`tablet_product_id` 모두 nullable — 제약은 프론트엔드 검증 로직에만 있었음. `OrderNew.tsx`의 `handleSubmit` 검증을 "유골함 필수"에서 "유골함 또는 위패 중 하나 이상 선택"으로 변경, 두 `ProductPicker` 라벨을 각각 "유골함 선택 (위패와 최소 1개 선택)" / "위패 선택 (유골함과 최소 1개 선택)"으로 수정
**결과:** 완료. `npx tsc --noEmit` 통과. Playwright로 실제 기존 주문(`736297dc-...`, 홍실동)의 주문 상세 화면에서 위패 "크게 보기" 클릭 → 모달에 확대된 각인 미리보기 정상 렌더링 확인. 다크모드 "화장 날짜 및 시간" 필드에서 달력 아이콘이 흰색으로 잘 보이는 것 확인. `/orders/new`에서 위패(금선세라믹위패)만 선택하고 유골함은 선택하지 않은 채 필수 항목(고인명/성함/장례식장/장지/전화번호) 입력 후 제출 → 주문 정상 접수("주문서가 접수되었습니다") 확인, DB에도 `urn_product_id=null`/`tablet_product_id`만 저장된 것 확인 후 테스트 주문 삭제.

## 22:35 작업 (주문 상세 화면에서 각인 적용된 상품 이미지 다운로드)
**요청:** "각인 이미지 다운로드 기능 계획대로 진행해줘" — 앞서 제시한 계획(Canvas로 상품 이미지+각인 텍스트+사진을 합성해 PNG로 다운로드) 승인.
**작업 내용:**
- `lib/composeEngraveImage.ts` 신규 — `<canvas>`에 상품 이미지를 `object-contain`과 동일한 비율로 그리고, 위패 사진(있는 경우)을 타원형으로 클립해 `object-cover`와 동일하게 오려 붙인 뒤, 텍스트 각인 요소를 그림. `EngravePreview`의 `writing-mode: vertical-rl`(세로쓰기)을 캔버스에서는 문자 단위로 한 글자씩 세로로 쌓아 그리는 방식으로 재현(`anchor: 'top'`/`'center'` 기준도 동일하게 반영). 커스텀 한글 웹폰트(나눔명조 등)는 `document.fonts.load()`로 미리 로드한 뒤 그려야 깨지지 않아 이를 먼저 대기하도록 처리. 크로스오리진 이미지(Supabase Storage) 로드 시 `crossOrigin='anonymous'` 지정
- `OrderDetail.tsx`: `downloading`/`downloadError` state와 `handleDownload(kind)` 핸들러 추가, 확대 보기 모달(`Modal`) 안 각 미리보기 하단에 "이미지 다운로드" 버튼 추가 — 클릭 시 캔버스 합성 → PNG Blob 생성 → `{고인명}_{유골함|위패}_각인.png`로 파일 다운로드
**결과:** 완료. `npx tsc --noEmit` 통과. Playwright로 실제 검증: ① 기존 주문(홍실동, 유골함 S꽃난황토)에서 "이미지 다운로드" 클릭 → PNG 정상 생성·다운로드, 열어보니 상품 이미지 위에 이름(중앙)·출생/사망 한자+연월일(좌우 대칭 세로쓰기)·종교기호가 화면 미리보기와 동일하게 합성됨 확인 ② 위패+사진 조합으로 신규 테스트 주문 생성(사진 업로드 포함) 후 다운로드 → 사진이 타원형으로 오려붙여진 상태까지 정상 합성 확인, Supabase Storage 이미지의 CORS 문제로 캔버스가 오염되는 문제(`toBlob` 실패)도 발생하지 않음 확인. 검증 후 테스트 주문 삭제.
